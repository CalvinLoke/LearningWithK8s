apiVersion: v1
kind: Service
metadata:
  name: minio-service
  namespace: minio-namespace
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: minio